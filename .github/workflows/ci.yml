name: Rust Simple CI

on: [push]

env:
  CARGO_TERM_COLOR: always

jobs:
  check-my-code:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run tests
        run: cargo test

      - name: Run my app
        run: cargo run -- live poem.txt
